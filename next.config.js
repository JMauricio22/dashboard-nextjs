/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['tailwindui.com', 'images.unsplash.com', 'placeimg.com', 'ui-avatars.com', 'placeimg.com/'],
  },
};

module.exports = nextConfig;
